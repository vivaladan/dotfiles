# dotfiles

Personal configuration files managed with symlinks.

## Setup

```bash
git clone git@github.com:vivaladan/dotfiles.git ~/dotfiles
cd ~/dotfiles
```

## Structure

Configuration files live in this repo and are symlinked to their expected locations (e.g. `~/.zshrc` → `~/dotfiles/.zshrc`).
